[toolchain]
# Pinned for reproducibility. The crate also compiles on 1.75 (see CI's msrv job);
# if you need that, run `rustup override set 1.75.0` in this directory.
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"
